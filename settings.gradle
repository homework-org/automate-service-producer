pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        mavenCentral()
    }
}

include(':commons')

project(":commons").projectDir = new File("../commons")

rootProject.name = 'automate-service-producer'
